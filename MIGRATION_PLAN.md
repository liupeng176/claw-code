# Kotlin 迁移初始计划

本分支用于逐步将项目迁移到 Kotlin。初始计划如下：

1. 在此分支中准备迁移计划文档与优先文件清单（TODO），但不修改当前构建或 CI；
2. 迁移策略：采用渐进式迁移，保持 Java/Kotlin 混编，逐模块迁移；
3. 首批优先转换目标（示例）：实体/DTO、service 层、controller 层、工具类；具体文件清单将在后续评估后补充；
4. 构建工具建议：Gradle Kotlin DSL，Kotlin 版本 1.9，目标 JVM 17；
5. 测试策略：保留现有测试，逐步将测试迁移到 Kotlin（可选使用 Kotest 与 MockK）；
6. Lombok：暂不移除以降低风险，后续逐步替换为 Kotlin 特性；
7. 后续步骤：在此分支基础上创建一系列小 PR，每个 PR 处理一个模块或一类变更（添加 Kotlin 支持、迁移实体、迁移服务、移除 Lombok 等）。

注意：本次仅创建分支与文档文件，不修改构建、CI 或其他代码。
